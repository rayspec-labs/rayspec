{
  "name": "@rayspec/file-runtime",
  "version": "0.0.0",
  "private": true,
  "license": "FSL-1.1-ALv2",
  "type": "module",
  "description": "The generic Tier-B file-ingest capability (file_input) — a bounded raw-byte upload plus a JSON submit that seals the file and emits a durable workflow trigger. Product-neutral; a record×audio hybrid (record_input donates the manifest/event/submit template, audio_input donates the stream-ingest byte transport).",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./rayspec": {
      "types": "./dist/rayspec/index.d.ts",
      "default": "./dist/rayspec/index.js"
    },
    "./manifest.json": "./manifest.json"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@rayspec/handler-sdk": "workspace:*",
    "@rayspec/spec": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.12.0",
    "typescript": "5.9.3",
    "vitest": "4.1.9"
  }
}
